# Configuration file for U-Boot environment tools
# Format: device_name    device_offset   env_size    flash_sector_size   num_sectors
# For QEMU, we use a file-based approach since there's no real flash

# U-Boot environment stored at end of U-Boot binary region
# This is a typical configuration for QEMU virtual machines
/tmp/uboot_env.bin      0x0000          0x4000      0x4000               1